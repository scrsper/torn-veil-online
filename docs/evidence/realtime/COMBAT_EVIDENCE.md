# Combat evidence index

Report: [Real-time combat contact and defense v0.1](../../REALTIME_COMBAT_CONTACT_DEFENSE_V0_1.md).
Production revision: `7f2f418fe8b5d992daba74c661ca8585e315c65f`.

- `combat-counterfactual-summary.json`: canonical A–I paired arena results, including both NPC
  timings; repetitive frame histories remain in the local original.
- `combat-regression-focused.json`: actual focused checks, earlier failures, subsequent stress
  success and independent review. Earlier timeouts are not counted as passes.
- `native-combat-automation-final.json`: 12 native tests, blocked reconciliation and video counts.
- `family-causal-followup.json`: injury knowledge arrives, but no care plan is selected for
  execution. Competing priorities explain most observed decisions. The 17-world-hour formation
  delay remains unexplained; no production fix is justified. The fixture experiment is uncommitted.
- `native-live-combat-repetitions.json`: native 30 attack/30 duck timing summary. Complete raw
  samples remain local in `.raw.json.gz`, uncommitted, with the original hash in the summary.
- `native-combat-checkpoint-summary.json`: existing rendered/no-capture diagnostics and bounded
  arena server observations, with source hashes. The late sidestep is active before contact;
  its final later duck is a separate input. No mismatched late server snapshots are used.
- `native-combat-*-rendered.{mp4,png}`: continuous timestamped videos and representative stills
  for sidestep/high-duck/low-duck; original per-frame diagnostic streams remain local.
  Readback captures approximately 10 distinct frames/second; these are not 60-fps latency recordings.
- `combat-transport-checkpoint-summary.json`: summary fields from five existing runs; original
  per-message timing records and bridge logs remain local. Includes the deadline-only experiment
  (no material improvement), isolated arena (40 combat + 1,870 movement commands, all applied),
  and matched synchronous/cooperative region projection. All nine initial regions and eight
  dynamic transfers were acknowledged. Each playable run has 40 combat terminal receipts,
  including one rejection. Legacy `appliedRoundTrip*` includes terminal rejections; its count
  is not the count of accepted actions.
- `combat-transport-stage-summary.json`: intervals computed from records carrying both stage
  timestamps. Application intervals exclude rejection records that have no application timestamp.
- `combat-cognition-profile.json`: accepted baseline, phase transcript regression, bounded cue
  fix and allocation optimization profiles. Final allocation change preserves the same tally.
- `combat-production-manifest.json`: exact production/test/script paths and shared-spec hash.

The native tests and captures preceded the final TypeScript cognition/allocation corrections.
Those later changes do not change native code or the tested controlled-arena geometry/timeline.
The last full suite was 851/852, not an acceptance pass. No new tests, native launch or benchmarks
were run for this documentation checkpoint. Raw originals are preserved locally, not staged.
No human approval,
two-rendered-client PvP or physical input-to-photon measurement is claimed.
